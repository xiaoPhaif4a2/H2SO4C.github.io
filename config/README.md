# Feature configuration

- `butterfly/` contains the editable source for Butterfly theme settings.
- `categorybar.yml` maps published and planned category names to their cover and
  description. Every published category needs a mapping; planned categories are
  displayed with a count of zero until their first article is published.
