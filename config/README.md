# Feature configuration

- `butterfly/` contains the editable source for Butterfly theme settings.
- `categorybar.yml` maps each published category name to its cover and
  description. Keep this mapping exact: the build fails if a category is missing
  or a mapping no longer has a matching category.
