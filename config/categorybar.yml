# Category cards are keyed by category name so their resources never depend on
# Hexo's category ordering.

enable: true
layout:
  type: id
  name: recent-posts
  index: 0
column: odd
row: 1

categories:
  过去的幻影:
    description: 过去的幻影
    cover: /images/covers/cover1.webp
